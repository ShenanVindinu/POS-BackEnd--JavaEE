create table customer(
                        id VARCHAR(50) primary key,
                        name VARCHAR(50),
                        address VARCHAR(20),
                        salary VARCHAR(50)
)

create table item(
                         id VARCHAR(50) primary key,
                         name VARCHAR(50),
                         qty VARCHAR(20),
                         price VARCHAR(50)
)

